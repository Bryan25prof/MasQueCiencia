PENDIENTE D (paso previo) — ACTIVACIÓN DOCENTE
================================================

Este paquete contiene ÚNICAMENTE los archivos modificados o nuevos de
este trabajo. Los archivos dentro de "MasQueCiencia_Beta_v1.0" reflejan
EXACTAMENTE la misma ruta que tienen en tu repositorio.

ARCHIVOS DEL REPOSITORIO (copiar a la misma ruta):
  MODIFICADOS:
  - index.html
  - js/shared/profiles.js
  - js/shared/profiles-ui.js

  NUEVO:
  - js/shared/teacher-activation.js

ARCHIVO SQL (NO va al repositorio de GitHub — se ejecuta directo en
Supabase, ver más abajo):
  - SUPABASE_MIGRATION_activacion_docente.sql

PASOS PARA DEJAR ESTO FUNCIONANDO:

1. Sube los 4 archivos del repositorio a las mismas rutas indicadas
   arriba (reemplazando los 3 existentes, agregando el nuevo).

2. Ejecuta SUPABASE_MIGRATION_activacion_docente.sql completo en el
   SQL Editor de tu proyecto de Supabase (Dashboard → SQL Editor →
   New query → pegar todo → Run). Es seguro, no toca nada existente.

3. IMPORTANTE — sin este paso la activación queda instalada pero
   inactiva: inserta tu código real (ver la sección 5 del reporte
   MQC_PENDIENTE_D_ACTIVACION_DOCENTE_REPORTE.md para el comando
   exacto). Nunca pegues tu código real en ningún chat ni documento —
   solo directamente en el SQL Editor de Supabase.

4. Confirma que tu MQC_ANALYTICS_CONFIG de producción (en
   js/shared/analytics-config.js de TU despliegue real, no de este
   repositorio de trabajo) tiene enabled:true con tu supabaseUrl y
   supabaseAnonKey reales. Sin eso, la activación no tiene con qué
   validar el código.

REPORTE COMPLETO:
  Ver MQC_PENDIENTE_D_ACTIVACION_DOCENTE_REPORTE.md (entregado junto
  con este paquete) para el detalle de la protección contra fuerza
  bruta, el procedimiento de rotación de códigos, el tratamiento de
  perfiles docentes ya existentes, y las pruebas realizadas (incluidas
  pruebas contra un Postgres real, no solo simuladas).

IMPORTANTE:
  - No se implementó todavía AccessControl académico ni las guardas de
    escritura del Pendiente D — solo este mecanismo de activación,
    tal como se pidió. Eso queda para el siguiente lote.
  - No se incluye ningún código docente real en ningún archivo de
    este paquete.
  - Pendientes A y B permanecen intactos y no fueron tocados.
